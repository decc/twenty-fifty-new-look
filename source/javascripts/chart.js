define(['knockout'], function(ko) {
  'use strict';

  var Chart = function(id, name) {
    var self = this;

    self.id = id;
    self.name = name;
    self.title = title;
    self.data = {};
  };

  Chart.prototype = {

  };

  return Chart;
});

